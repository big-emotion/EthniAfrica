# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules
.pnp
.pnp.js

# Testing
coverage
*.lcov
.nyc_output

# Playwright
playwright-report/
test-results/
.playwright-cache/
# The Playwright MCP drops console logs and page snapshots into the cwd of every
# agent session. `*.log` already hid the logs; the `page-*.yml` snapshots kept
# surfacing as untracked litter in the main checkout.
.playwright-mcp/
storybook-static/

# Next.js
.next
out
.next/cache
.vercel
.turbo
# Regenerated by every `next dev` / `next build` / `next typegen`, and the two
# commands write different contents, so tracking it dirtied the tree on each run.
# Next's own docs say not to version it; `tsc` passes without it.
next-env.d.ts

# Production
dist
dist-ssr
build

# Environment variables
# Every env file is a secret until proven otherwise. Listing variants one by
# one left `.env.production`, `.env.development` and `.env.test` addable; the
# two templates are the only env files this repository tracks.
.env
.env.*
!.env.example

# TypeScript
*.tsbuildinfo
*.tsbuildinfo.*

# Claude Code
# Local settings stay out; the project's own skills are versioned, so each one
# is re-included explicitly. Personal and plugin-installed skills land in
# .claude/skills/ too, which is why the default is ignore-everything.
.claude/*
# settings.json carries the shared PostToolUse lint hook, so it is part of the
# project standard rather than a personal preference; settings.local.json stays
# ignored by the rule above.
!.claude/settings.json
!.claude/skills/
.claude/skills/*
!.claude/skills/afrik-art-director/
!.claude/skills/afrik-curator/
!.claude/skills/afrik-game-designer/
!.claude/skills/afrik-translator/
!.claude/skills/ethniafrica-audience-audit/
!.claude/skills/ethniafrica-audit/
!.claude/skills/ethniafrica-bootstrap-confluence/
!.claude/skills/ethniafrica-content-strategist/
!.claude/skills/ethniafrica-essai/
!.claude/skills/ethniafrica-idee/
!.claude/skills/ethniafrica-infra/
!.claude/skills/ethniafrica-message/
!.claude/skills/ethniafrica-mythe/
!.claude/skills/ethniafrica-onomastique/
!.claude/skills/ethniafrica-produire/
!.claude/skills/ethniafrica-reseaux-help/
!.claude/skills/ethniafrica-structure/
!.claude/skills/ethniafrica-experience-optimizer/
!.claude/skills/ethniafrica-release/
!.claude/skills/ethniafrica-spec/
!.claude/skills/ethniafrica-ticket/
.entire/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.cursor
*.swp
*.swo
*~
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS
Thumbs.db
.DS_Store
**/.DS_Store
*.DS_Store

# Misc
*.local
.cache
.temp
.tmp

# Dataset
dataset/*
!dataset/source/
!dataset/source/**/*
# Translated records mirror the source tree per locale (REQ-146)
!dataset/translations/
!dataset/translations/**/*
# Ignorer les fichiers système dans dataset
dataset/**/.DS_Store
dataset/**/*.DS_Store
# Ignorer les dossiers générés
dataset/matched/
dataset/parsed/

# Archives supprimées
dataset/source/archive/

# Logs générés (migration, validation)
dataset/source/afrik/logs/
# Agent runtime mirrors (local skill caches, not project source)
.agents/
.codex/
# Media renders and agent scratch produced in the checkout, never project
# source. The approved media library lives outside the repository.
output/

# The social render engine is Python. Its bytecode caches and its virtualenv are
# build products of a machine, not source; `social/harness/requirements.txt` is
# what actually pins the dependencies.
__pycache__/
*.pyc
social/harness/venv/
Claude outputs/
skills-lock.json
.env*
!.env.example
